# Managed by puppet
# Filter to modify output
[FILTER]
    # configure plugins
    Name parser<%- if @match %>
    Match <%= @match -%>
<% end -%>
    key_name <%= @key_name %>
    parser <%= @parser %>

